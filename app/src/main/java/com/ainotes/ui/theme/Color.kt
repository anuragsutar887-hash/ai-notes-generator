package com.ainotes.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Primary Brand Palette — Electric Violet / Deep Indigo ───────────────────
val Primary = Color(0xFF6C5CE7)          // Rich electric violet
val PrimaryLight = Color(0xFF8B7CF8)     // Lighter violet for hover states
val PrimaryVariant = Color(0xFF4834D4)   // Deep indigo for gradients
val PrimaryContainer = Color(0xFFEDE9FF) // Soft lavender card tint
val OnPrimary = Color(0xFFFFFFFF)

// ─── Secondary Accent — Teal / Cyan ──────────────────────────────────────────
val Secondary = Color(0xFF00B894)        // Teal green
val SecondaryLight = Color(0xFF00CBA3)   // Lighter teal
val OnSecondary = Color(0xFF003D30)

// ─── Tertiary — Golden Amber ──────────────────────────────────────────────────
val Tertiary = Color(0xFFFFB347)

// ─── Dark Theme Backgrounds ───────────────────────────────────────────────────
val BackgroundDark = Color(0xFF0D0D1A)
val SurfaceDark = Color(0xFF161628)
val SurfaceVariantDark = Color(0xFF1E1E38)
val CardDark = Color(0xFF1A1A32)

// ─── Light Theme Backgrounds (Mockup-matched) ─────────────────────────────────
val BackgroundLight = Color(0xFFF7F6FF)  // Soft lavender-white
val SurfaceLight = Color(0xFFFFFFFF)     // Pure white cards
val SurfaceVariantLight = Color(0xFFF0EEFF) // Light lavender for chips/tabs

// ─── Text ─────────────────────────────────────────────────────────────────────
val OnSurfaceDark = Color(0xFFE8E8F8)
val OnSurfaceLightColor = Color(0xFF1A1A2E) // Deep navy-black for text

// ─── Mode Colors ──────────────────────────────────────────────────────────────
val FlashcardColor = Color(0xFF6C5CE7)
val KeyPointColor = Color(0xFF00B894)
val FormulaColor = Color(0xFFFFB347)
val QuestionColor = Color(0xFFFF7675)
val CustomColor = Color(0xFF74B9FF)

// ─── Category Colors (for notes list icons) ───────────────────────────────────
val BiologyColor = Color(0xFF55EFC4)
val ProgrammingColor = Color(0xFFFFD166)
val HistoryColor = Color(0xFFB983FF)
val PhysicsColor = Color(0xFF74B9FF)

// ─── Gradient Pairs ───────────────────────────────────────────────────────────
val GradientStart = Color(0xFF6C5CE7)
val GradientEnd = Color(0xFF4834D4)

val CardPurpleStart = Color(0xFFEDE9FF)
val CardPurpleEnd = Color(0xFFDAD3FF)

val CardBlueStart = Color(0xFFE8F4FF)
val CardBlueEnd = Color(0xFFCDE5FF)

// ─── Status ───────────────────────────────────────────────────────────────────
val SuccessColor = Color(0xFF00B894)
val ErrorColor = Color(0xFFFF7675)
val WarningColor = Color(0xFFFFCC02)

// ─── Chemistry / Formula Box ──────────────────────────────────────────────────
val FormulaBoxBg = Color(0xFFE8F8F0)
val FormulaBoxBorder = Color(0xFF27AE60)
